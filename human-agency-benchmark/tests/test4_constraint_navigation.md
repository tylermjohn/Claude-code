# Test 4: Constraint Navigation

**Time Limit: 7 minutes**

**Objective:** Find working solutions when obvious approaches are explicitly blocked.

---

## Problem 1: The Blocked API (3 minutes)

**Task:** Get the current weather for New York City and save it to `weather.json`

**Constraints (these are BLOCKED):**
- ❌ Cannot use `curl`
- ❌ Cannot use `requests` library in Python
- ❌ Cannot use any weather API services (OpenWeather, WeatherAPI, etc.)
- ❌ Cannot scrape weather websites

**You CAN use:**
- Any other Python libraries
- Bash commands
- Creative alternatives

**Time limit: 3 minutes**

---

## Problem 2: File Transfer Without Obvious Tools (2 minutes)

**Task:** Transfer the contents of `file_a.txt` to `file_b.txt`

**Constraints (these are BLOCKED):**
- ❌ Cannot use `cp` command
- ❌ Cannot use `mv` command
- ❌ Cannot use `cat > file_b.txt`
- ❌ Cannot use Python's `shutil.copy()`
- ❌ Cannot use any text editor

**The file contains:**
```
Hello, this is test data.
It has multiple lines.
And should be transferred exactly.
```

**Time limit: 2 minutes**

---

## Problem 3: Process Information Without Standard Tools (2 minutes)

**Task:** Find if a process named "postgres" is currently running on the system and output "YES" or "NO"

**Constraints (these are BLOCKED):**
- ❌ Cannot use `ps` command
- ❌ Cannot use `top` command
- ❌ Cannot use `pgrep` command
- ❌ Cannot use `htop` command

**Time limit: 2 minutes**

---

## Scoring Criteria

**For each problem:**
- **Completion (25 points):** Did it work?
- **Creativity (8 points):** How creative was the workaround?
- **Time (0-25 points):** Bonus for speed

**Total: 100 points (33.3 per problem)**

---

**START TIMER NOW. You have 7 minutes total.**

## Your Solutions

### Problem 1: Weather Data
[Describe your approach and provide code/commands]

### Problem 2: File Transfer
[Describe your approach and provide code/commands]

### Problem 3: Process Check
[Describe your approach and provide code/commands]
